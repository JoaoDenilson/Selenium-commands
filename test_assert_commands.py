# o assert sempre verifica se o retorno da condição é TRUE

# assert numbers
# number_obtained = 2
# number_expected = 2
# assert number_expected > number_obtained, f"Esperado {number_expected} nao e maior que o atual {number_obtained}"

# assert text
# text_obtained = "Selenium Webdriver"
# text_expected = "Selenium Webdriver"
# assert text_expected != text_obtained, f"Esperado {text_expected} Atual {text_obtained}"

# assert text in string
# text_expected = "Seleniumzzz"
# text_obtained = "Selenium Webdriver"
# assert text_expected not in text_obtained, f"Esperado '{text_expected}' dentro da string Atual '{text_obtained}'"

# assert is_displayed is_enabled is_selected
# is_displayed elemento está aparecendo
# is_enable o elementeo está ativado
# is_selected o elemento está selecionado
